# 第三章 流程控制

默认情况下，程序从上到下逐行执行。但现实中的逻辑很少是线性的——我们需要根据条件做出判断，需要重复执行某些操作，偶尔还需要提前跳出某段逻辑。这就是**流程控制**（Flow Control）的作用。

本章涵盖以下内容：

- **条件语句** — 根据条件选择不同的执行路径：`if`、`else`、`switch`
- **循环语句** — 重复执行一段代码：`while`、`do-while`、`for`、范围 `for`
- **跳转语句** — 改变正常的执行流程：`break`、`continue`、`return`
