# 第二章 基本概念

编程的本质是对数据的处理。要处理数据，首先需要知道如何存储数据（变量）、数据有哪些种类（数据类型）、如何对数据进行计算（运算符）、如何与用户交互（输入输出），以及不同类型的数据之间如何转换（类型转换）。

本章涵盖以下内容：

- **变量与数据类型** — 如何声明变量，C++ 提供了哪些基本数据类型
- **运算符与表达式** — 算术、关系、逻辑等运算符的用法和优先级
- **输入与输出** — 使用 `std::cin` 和 `std::cout` 实现用户交互
- **类型转换** — 隐式转换与显式转换的规则和注意事项
