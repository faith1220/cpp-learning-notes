# 第五章 数组与字符串

前几章中我们处理的都是单个变量。但实际编程中经常需要处理一组相关的数据——一组成绩、一串文字、一批记录。**数组**（Array）和**字符串**（String）就是用来存储和操作这类数据的基本工具。

本章涵盖以下内容：

- **数组** — C++ 原生数组的声明、初始化、访问和局限性
- **C 风格字符串** — 以空字符结尾的字符数组，了解其历史意义和潜在风险
- **std::string** — C++ 标准库提供的字符串类，安全且功能丰富
- **std::array 与 std::vector 初步** — 标准库容器对原生数组的现代替代方案
